/**
 * The registered channel id.
 *
 * Lives on its own so provider adapters can stamp it without importing the
 * normalizer of a different provider.
 */
export const IMESSAGE_CHANNEL = "imessage";
