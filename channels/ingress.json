{
  "routes": [
    {
      "path": "events",
      "kind": "http",
      "description": "Inbound message webhooks from the configured provider's line — Comms by Osis (message.received) or Photon (messages). Signature-verified; rejects every delivery when no signing secret is stored."
    }
  ]
}
